{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"build": "wrangler publish --dry-run --outdir=dist",
		"deploy": "wrangler publish",
		"dev": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^2.0.0"
	}
}
